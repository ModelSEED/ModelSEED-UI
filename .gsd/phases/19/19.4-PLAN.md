---
phase: 19
plan: 4
wave: 1
---

# Phase 19.4 Plan: DataControlHeader Integration & Search Fix

## Problem
The DataControlHeader component exists but needs:
1. Search functionality to be properly wired (currently uses onSearchChange but filtering logic may not be integrated)
2. Consistent integration across user data pages (My Models, My Media)
3. Integration into biochem reference data tabs (compounds, reactions)

## Tasks

<task type="auto">
  <name>Fix DataControlHeader search functionality</name>
  <files>components/layout/DataControlHeader.tsx</files>
  <action>Verify the TextField input is working. The component receives searchValue and onSearchChange props correctly. Ensure the input is clickable, focused, and the value updates on typing. No changes may be needed if props are wired correctly.</action>
  <verify>Test that typing in the search field updates the value and triggers onSearchChange callback.</verify>
  <done>DataControlHeader search input is fully functional</done>
</task>

<task type="auto">
  <name>Integrate DataControlHeader into My Models</name>
  <files>app/(user-data)/my-models/page.tsx</files>
  <action>Replace the existing TextField search implementation with DataControlHeader component. Import DataControlHeader and replace lines 166-174 with the component. Ensure filtering logic uses search value from state.</action>
  <verify>My Models page displays DataControlHeader with search that filters the table.</verify>
  <done>My Models uses consistent DataControlHeader component</done>
</task>

<task type="auto">
  <name>Integrate DataControlHeader into My Media</name>
  <files>app/(user-data)/myMedia/page.tsx</files>
  <action>Replace the existing TextField search implementation with DataControlHeader component. Import DataControlHeader and replace lines 121-129 with the component.</action>
  <verify>My Media page displays DataControlHeader with search that filters the table.</verify>
  <done>My Media uses consistent DataControlHeader component</done>
</task>

<task type="auto">
  <name>Integrate DataControlHeader into Biochem Compounds</name>
  <files>app/(reference-data)/biochem/compounds/page.tsx</files>
  <action>Import DataControlHeader and add it above the DataGrid. Add local search state and filtering logic to filter the compounds data.</action>
  <verify>Biochem Compounds page shows DataControlHeader above table with functional search.</verify>
  <done>Biochem Compounds has consistent search header</done>
</task>

<task type="auto">
  <name>Integrate DataControlHeader into Biochem Reactions</name>
  <files>app/(reference-data)/biochem/reactions/page.tsx</files>
  <action>Import DataControlHeader and add it above the DataGrid. Add local search state and filtering logic to filter the reactions data.</action>
  <verify>Biochem Reactions page shows DataControlHeader above table with functional search.</verify>
  <done>Biochem Reactions has consistent search header</done>
</task>

<task type="auto">
  <name>Verify all pages build correctly</name>
  <files>Multiple</files>
  <action>Run npm run build to ensure all changes compile without errors.</action>
  <verify>Build succeeds with no errors.</verify>
  <done>All changes compile</done>
</task>

## Timestamp Log
- Created: 2026-03-12 17:40:00 -05:00
