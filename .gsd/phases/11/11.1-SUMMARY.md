---
phase: 11
plan: 1
status: complete
---

# Summary 11.1: PlantSEED Maintenance Warning

## What Was Done
- Added a filled warning `Alert` banner to the Build Model page (`/plant`) with the message:
  "PlantSEED is being updated to version 3.0. Annotation and reconstruction services are temporarily offline..."
- Disabled the "UPLOAD Plants FASTA" tab via `disabled={PLANTSEED_MAINTENANCE}` flag.
- Added an outlined info `Alert` banner to the Genomes reference page (`/genomes`) informing users of the v3.0 update while keeping the table operational.
- Introduced a `PLANTSEED_MAINTENANCE` boolean constant — flip to `false` when PlantSEED v3 is ready.

## Files Modified
- `app/(build-model)/plant/page.tsx` — Banner + disabled tab
- `app/(reference-data)/genomes/page.tsx` — Info banner

## Verification
- `/genomes` renders banner and data table correctly (screenshot confirmed).
- `/biochem/reactions` still loads with no regressions.

## Timestamp Log
- Created: 2026-03-11 09:55:00 -0500
