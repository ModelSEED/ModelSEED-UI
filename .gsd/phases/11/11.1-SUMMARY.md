# 11.1-SUMMARY.md

## Phase 11: Service Layer Transition & PlantSEED Maintenance

**Objective:** Align the frontend with the backend "Proxy-All" strategy and handle the PlantSEED v3 rollout strategy.

**Key Deliverables:**
*   **PlantSEED Banner & Guardrails**: Implement a global maintenance banner for `/plant` and Genomes pages. Disable "Build Model" submissions for plant genomes to prevent usage of legacy annotation pipelines.
*   **API Proxy Audit**: Audit all calls in `lib/api/workspace.ts`. Ensure they are configured to point to the new unified proxy layer (sheltering the UI from direct Workspace service dependencies).
*   **Service Integration**: Move standard biochemistry lookups (`get_reactions`, `get_compounds`) to the new service while maintaining the legacy link for RAST job statuses as directed by the modeling team.

## Timestamp Log
- Created: 2026-03-11 09:44:00 -0500
