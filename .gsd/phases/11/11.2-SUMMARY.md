---
phase: 11
plan: 2
status: complete
---

# Summary 11.2: Service Layer Proxy Strategy

## What Was Done
- Created `lib/api/config.ts` — centralized API configuration with:
  - `USE_NEW_PROXY` boolean toggle (default: `false`)
  - Workspace endpoint: legacy (`p3.theseed.org`) vs proxy (`modelseed.org/api/workspace`)
  - Solr endpoint: legacy (`modelseed.org/solr/`) vs proxy (`modelseed.org/api/solr/`)
  - ProbModelSEED endpoint: legacy vs proxy
  - `MODELSEED_SUPPORT_URL` — hardcoded to legacy, explicitly excluded from proxy toggle per Chris Henry's directive
  - `CPD_IMG_BASE` — compound image URL
- Refactored `lib/api/workspace.ts` to import `WORKSPACE_URL` from config instead of hardcoding.
- Refactored `lib/api/biochem.ts` to import `SOLR_BASE` and `CPD_IMG_BASE` from config instead of hardcoding.

## Files Created
- `lib/api/config.ts`

## Files Modified
- `lib/api/workspace.ts` — endpoint now config-driven
- `lib/api/biochem.ts` — endpoint now config-driven

## Verification
- `/biochem/reactions` loads data correctly through the refactored config pipeline (screenshot confirmed).
- All endpoints resolve identically to pre-refactor values when `USE_NEW_PROXY = false`.

## Timestamp Log
- Created: 2026-03-11 09:55:00 -0500
